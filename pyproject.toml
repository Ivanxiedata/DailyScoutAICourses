[project]
name = "automated-ai-journal-mvp"
version = "1.0.0"
description = "Plug-and-play OpenClaw AI journal pipeline"
requires-python = ">=3.11"
dependencies = [
  "fastapi==0.116.1",
  "uvicorn==0.35.0",
  "pydantic==2.11.7"
]

[tool.ruff]
line-length = 100
target-version = "py311"
