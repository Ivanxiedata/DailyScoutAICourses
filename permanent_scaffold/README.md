# permanent_scaffold

Reusable full-stack template for every generated AI journal app.

- `backend_fastapi`: generic runtime endpoint that delegates logic to plugin
- `frontend_nextjs`: generic UI renderer based on plugin schema
